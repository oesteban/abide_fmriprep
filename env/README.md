# Local environment files (not versioned)

Place local-only secrets under:

  env/secrets/

Expected files:

  env/secrets/fs_license.txt

These paths are ignored by Git via the root .gitignore to prevent
accidental sharing of sensitive data.

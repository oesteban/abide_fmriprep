# Environment paths for ABIDE analysis pipeline.
# Sourced by all SLURM sbatch scripts and the orchestration driver.
# Cluster-specific paths are resolved via cluster_config.sh.
source "$(dirname "$0")/cluster_config.sh"
